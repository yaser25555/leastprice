name: leastprice
description: LeastPrice Flutter application
publish_to: "none"
version: 1.0.0+2

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.1.3
  connectivity_plus: ^7.1.0
  firebase_auth: ^6.1.1
  firebase_core: ^4.6.0
  http: ^1.2.2
  share_plus: ^10.0.2
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/products.json
